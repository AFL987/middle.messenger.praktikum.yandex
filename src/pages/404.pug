extends ../layout/main

block vars
    - var pageTitle = '404'

    include ../components/empty_space/empty_space.pug

block content
    .containerCenter.auxiliary_page
        .title_big.text_center 404
        +emptySpace("20")
        .title_middle.text_center Не туда попали
        +emptySpace("60")
        .text_center
            a(href="./chat.pug") Назад к чатам
